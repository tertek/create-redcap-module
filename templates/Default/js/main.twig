/**
 * {{ options.moduleName }} - a REDCap External Module
 * Author: {{ options.author }}
*/

var {{ options.namespace }}_{{ options.moduleNameCC }} = {{ options.namespace }}_{{ options.moduleNameCC }} || {};
 
// Initialization
{{ options.namespace }}_{{ options.moduleNameCC }}.init = function() {

    console.log("Module has been initialized");

}